@using School.Models.WebModels.NotificationModels;
@using System.Text.Json;
@using System.Text.Json.Serialization;
@model NotificationInformation

<div class="dropdown topbar-head-dropdown ms-3 header-item">
    <button type="button" class="btn btn-icon btn-topbar btn-ghost-secondary rounded-circle" id="page-header-cart-dropdown" data-bs-toggle="dropdown" data-bs-auto-close="outside" aria-haspopup="true" aria-expanded="false">
        <i class="fa-regular fa-bell"></i>
        <span class="position-absolute topbar-badge cartitem-badge fs-10 translate-middle badge rounded-pill bg-danger">@Model.NotificationCount</span>
    </button>
    <div class="dropdown-menu dropdown-menu-xl dropdown-menu-end p-0 dropdown-menu-notification " aria-labelledby="page-header-cart-dropdown">

        <div class="message-box">
            <div class="item-header">
                <h6 class="item-title">@Model.NotificationCount Notification</h6>
            </div>
            <div class="message-bob-body">
                <div class="item-content">

                    @if (Model.NotificationCount <= 0)
                    {
                        <div class="media">
                            <div class="media-body space-sm">
                                <div class="post-title">Hey! No record to update</div>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="media" id="notificationBox_0">
                            <div class="item-icon bg-skyblue">
                                <i class="fas fa-check"></i>
                            </div>
                            <div class="media-body space-sm">
                                <div class="post-title">@Model.NotificationCount </div>
                                <span>Record Updated</span>
                            </div>
                        </div>
                        <div class="my-3 text-center view-all" style="display: block;">
                            <a class="btn btn-soft-success waves-effect waves-light" href="@Url.Action("NotificationList","Student")">
                                View All Notifications <i class="ri-arrow-right-line align-middle"></i>
                            </a>
                        </div>
                       @* <div>
                            @Html.ActionLink("View All", "NotificationList", "Student", null, new { @class = "btn btn-dark btn-set-task w-sm-100" })
                        </div>*@
                    }
                </div>
            </div>
        </div>
    </div>
</div>